# Earlier house runs

| Claude Code · Fable 5.1 | Codex · Astra |
| --- | --- |
| ![Claude Code result](claude-code.png) | ![Codex draft](codex-draft.png) |

**Claude Code, test 5:** approximately **30 minutes of active work**, as reported
by the project author. The session log spans a longer period that includes idle
gaps; 30 minutes is not its full elapsed session duration. The image is that run's
`comparison.png`. The recorded model identifier was `claude-fable-5-1`.

**Codex, test 8:** approximately **8–9 minutes to the displayed draft**, using
Astra (`gpt-6-astra`). The image is that run's `final-draft.png`, generated around
8 minutes 48 seconds after the request. The later final deliverable took longer.
That run used plugin 0.7.1 and its photographic checks still needed refinement.

These are selected outputs from different development runs, not matched trials
or a general Claude-versus-Codex performance claim. Hardware, detail, interruptions,
model settings and plugin version affect time. These renders were generated during
the runs; they are not enhanced or newly generated for this README.
